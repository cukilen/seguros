<UserControl x:Class="Seguros.App.Vistas.ImportacionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="Elegir archivo (XLS o PDF)..." Click="BtnElegirArchivo_Click" Margin="0,0,12,0"/>
            <TextBlock x:Name="TxtArchivo" VerticalAlignment="Center" Foreground="Gray"/>
        </StackPanel>

        <TextBlock x:Name="TxtMensaje" Grid.Row="1" Foreground="DarkRed" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <GroupBox x:Name="PanelMapeo" Header="Mapeo de columnas del archivo" Grid.Row="2" Padding="8" Margin="0,0,0,8" Visibility="Collapsed">
            <StackPanel>
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,8" Foreground="Gray"
                           Text="Revisá que cada columna del archivo esté asociada al campo correcto del sistema. Elegí '(no importar)' para ignorar una columna."/>
                <ItemsControl x:Name="ListaMapeo">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <TextBlock Text="{Binding Columna}" Width="180" VerticalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock Text="→" Width="24" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <ComboBox Width="200" ItemsSource="{Binding CamposDisponibles}"
                                          SelectedItem="{Binding CampoSeleccionado, Mode=TwoWay}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Content="Generar vista previa" HorizontalAlignment="Left" Margin="0,8,0,0" Click="BtnGenerarVistaPrevia_Click"/>
            </StackPanel>
        </GroupBox>

        <DataGrid x:Name="Grid" Grid.Row="3" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fila" Binding="{Binding Item.NumeroFila}" IsReadOnly="True" Width="40"/>
                <DataGridTextColumn Header="Documento" Binding="{Binding Item.Documento}" IsReadOnly="True" Width="90"/>
                <DataGridTextColumn Header="Asegurado" Binding="{Binding Item.NombreAsegurado}" IsReadOnly="True" Width="140"/>
                <DataGridTextColumn Header="Compañía" Binding="{Binding Item.CompaniaNombre}" IsReadOnly="True" Width="110"/>
                <DataGridTextColumn Header="Ramo" Binding="{Binding Item.RamoTexto}" IsReadOnly="True" Width="90"/>
                <DataGridTextColumn Header="Póliza" Binding="{Binding Item.NumeroPoliza}" IsReadOnly="True" Width="80"/>
                <DataGridTextColumn Header="Prima" Binding="{Binding Item.PrimaTexto}" IsReadOnly="True" Width="70"/>
                <DataGridTextColumn Header="Error" Binding="{Binding Item.ErrorDeteccion}" IsReadOnly="True" Width="160"/>
                <DataGridCheckBoxColumn Header="¿Duplicado?" Binding="{Binding Item.EsPosibleDuplicado}" IsReadOnly="True" Width="80"/>
                <DataGridComboBoxColumn Header="Acción" SelectedItemBinding="{Binding Accion}" ItemsSource="{Binding AccionesDisponibles}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="Confirmar importación" Click="BtnConfirmar_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
